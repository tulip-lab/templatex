# TULIP Lab Slidev Course Template

Edit `slides.md`, then use the standard project commands:

```sh
pnpm install
pnpm check
pnpm dev
pnpm build
```

The Course profile includes the optional TULIP live audience addon.
